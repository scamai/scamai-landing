Run the TypeScript type checker and fix any errors found.

1. Run `npx tsc --noEmit` to check for type errors
2. If errors are found, read the relevant files and fix them
3. Re-run the type checker to confirm all errors are resolved
